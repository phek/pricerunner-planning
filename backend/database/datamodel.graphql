type User {
  id: ID! @unique
  name: String!
  email: String! @unique
  password: String!
  options: [Option!]!
}

type Option {
  id: ID! @unique
  createdAt: DateTime!
  description: String!
  postedBy: User
}